import { PageShell } from "@/components/ui/PageShell";

export function Writing() {
  return (
    <PageShell title="Writing">
      <p
        style={{
          fontFamily: "var(--sans)",
          fontSize: "0.88rem",
          color: "var(--fg-dim)",
          fontWeight: 300,
          lineHeight: 1.7,
          maxWidth: "560px",
          fontStyle: "italic",
        }}
      >
        Nothing here yet.
      </p>
    </PageShell>
  );
}
